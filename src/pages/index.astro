---
import Base from "../layouts/Base.astro";
import { site } from "../site.js";
const c = site.colors;

const features = [
  {
    title: "Protect your property, not just patrol it",
    body: "Stop relying on luck and locked doors. Our licensed guards deter theft, vandalism and trespass before they happen, with clear incident reports so you always know what's going on at your site.",
    img: "/assets/feature-property.webp",
    alt: "Security guard monitoring a Melbourne commercial property entrance",
    link: { label: "Explore security guards", href: "/security-guards-melbourne/" },
    rev: false,
  },
  {
    title: "Licensed, vetted guards — every shift",
    body: "Every officer holds a current Victorian security licence, passes a national police check, and is fully insured. You get professional, uniformed people who represent your business well, not a faceless agency.",
    img: "/assets/feature-guard.webp",
    alt: "Confident licensed security officer in uniform",
    link: { label: "See how we're licensed & insured", href: "/licensing-compliance/" },
    rev: true,
  },
  {
    title: "24/7 monitoring and rapid response",
    body: "Our control room never sleeps. With GPS-verified mobile patrols and alarm response across Greater Melbourne, we can get a guard on site fast and confirm a deployment window the moment you call.",
    img: "/assets/feature-response.webp",
    alt: "Security patrol vehicle on duty in Melbourne at dusk",
    link: { label: "Discover mobile patrols", href: "/mobile-patrol-security-melbourne/" },
    rev: false,
  },
];

const faqs = [
  { q: "Are your security guards licensed in Victoria?", a: "Yes. Every guard holds a current Victorian security licence and is police-checked. We also hold a Private Security Business Licence you can verify with the Victoria Police Licensing and Regulation Division at any time." },
  { q: "How quickly can you get a security guard on site?", a: "We can usually get a licensed guard on site within hours, not days, because we run a 24/7 dispatch team across Melbourne. For urgent jobs we'll confirm a deployment window the moment you call." },
  { q: "How much does it cost to hire a security guard in Melbourne?", a: "Cost depends on guard type, shift length and timing. We publish transparent hourly ranges so you can budget before you call, then give you an itemised quote with no surprises." },
  { q: "Can I hire a security guard for one day or a short-term event?", a: "Yes. We cover one-off events, short-term cover and ongoing contracts. Tell us the dates and site and we'll scope the right number of guards for the job." },
  { q: "Do you provide security for the Christmas and event season?", a: "Yes. Spring and summer events, retail peaks and end-of-year functions are our busiest period. Book early, because licensed crowd controllers fill up fast across Melbourne in November and December." },
  { q: "Are your guards insured and police-checked?", a: "Yes. We carry public liability insurance and every officer passes a national police check before they work a single shift, so your business and your visitors are properly protected." },
];

// Inline SVG icons
const icons = {
  guard: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M12 2l7 3v6c0 5-3.5 8.5-7 10-3.5-1.5-7-5-7-10V5l7-3z"/><circle cx="12" cy="10" r="2.2"/><path d="M8.5 16c.6-2 2-3 3.5-3s2.9 1 3.5 3"/></svg>',
  patrol: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M3 13l2-5h11l3 4h2v3h-2"/><circle cx="7" cy="17" r="2"/><circle cx="17" cy="17" r="2"/><path d="M5 17H4v-1m11 1H9"/></svg>',
  event: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><circle cx="9" cy="8" r="2.4"/><circle cx="16" cy="9" r="2"/><path d="M3 19c0-3 2.7-5 6-5s6 2 6 5M14.5 19c0-2 1-3.4 2.5-3.9"/></svg>',
  building: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><rect x="4" y="3" width="10" height="18" rx="1"/><path d="M14 9h6v12h-6M7 7h2M7 11h2M7 15h2"/></svg>',
};

const schema = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": ["Organization", "SecurityService", "LocalBusiness"],
      "@id": site.url + "/#org",
      name: site.brand,
      url: site.url,
      telephone: site.phone,
      areaServed: "Melbourne, Victoria, Australia",
      address: { "@type": "PostalAddress", addressLocality: "Melbourne", addressRegion: "VIC", addressCountry: "AU" },
      description: "Licensed Melbourne security company providing security guards, mobile patrols, event and commercial security across Greater Melbourne.",
    },
    {
      "@type": "WebSite",
      "@id": site.url + "/#website",
      url: site.url,
      name: site.brand,
      publisher: { "@id": site.url + "/#org" },
    },
    {
      "@type": "FAQPage",
      mainEntity: faqs.map(f => ({ "@type": "Question", name: f.q, acceptedAnswer: { "@type": "Answer", text: f.a } })),
    },
  ],
};
---
<Base>
  <script type="application/ld+json" slot="head" set:html={JSON.stringify(schema)} />

  <!-- HEADER -->
  <header class="site-header">
    <div class="container nav">
      <a href="/" aria-label={site.brand + " home"}>
        <svg class="logo" viewBox="0 0 250 50" role="img" aria-label={site.brand}>
          <path d="M16 4l13 5v11c0 9-6.5 15.5-13 18-6.5-2.5-13-9-13-18V9l13-5z" fill="#3B3B25"/>
          <path d="M9.5 19.5l4.5 4.5 9-10" fill="none" stroke="#C7D43F" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
          <text x="40" y="26" font-family="Fraunces, Georgia, serif" font-size="21" font-weight="600" fill="#3B3B25" letter-spacing="1">SENTINEL</text>
          <text x="41" y="40" font-family="Inter, sans-serif" font-size="8.2" font-weight="600" fill="#6B6B57" letter-spacing="3">SECURITY · MELBOURNE</text>
        </svg>
      </a>
      <nav class="nav-links">
        {site.nav.map(n => <a href={n.href}>{n.label}</a>)}
      </nav>
      <div class="nav-cta">
        <a class="btn btn-ghost" href={site.phoneHref}>Call {site.phone}</a>
        <a class="btn btn-primary" href="/contact/">Get a Free Quote</a>
      </div>
      <button class="menu-btn" aria-label="Menu">&#9776;</button>
    </div>
  </header>

  <!-- HERO -->
  <section class="hero">
    <div class="container hero-grid">
      <div>
        <h1>Melbourne security<br/>that protects<br/>what <em class="u-underline">matters</em></h1>
        <p class="lead">Forget unreliable guards and locked-up worries. Sentinel gives you licensed, insured, police-checked security that's on call 24/7, so your people, property and events stay safe.</p>
        <div style="display:flex;gap:14px;flex-wrap:wrap">
          <a class="btn btn-primary" href="/contact/">Get a Free Quote</a>
          <a class="btn btn-ghost" style="border-color:var(--lime);color:var(--cream)" href={site.phoneHref}>Talk to our team</a>
        </div>
      </div>
      <div class="hero-media">
        <img src="/assets/hero.webp" width="720" height="540" alt="Licensed Sentinel security guard outside a modern Melbourne building" />
        <div class="hero-badge">
          <strong>24/7</strong>
          <span>Licensed guards on call across Greater Melbourne</span>
        </div>
      </div>
    </div>
  </section>

  <!-- TRUST STRIP -->
  <div class="container trust">
    <span class="eyebrow">Protecting Melbourne businesses, sites and events</span>
    <div class="trust-row">
      <span class="trust-pill">Victorian Licensed</span>
      <span class="trust-pill">Fully Insured</span>
      <span class="trust-pill">Police-Checked Guards</span>
      <span class="trust-pill">ASIAL Standards</span>
      <span class="trust-pill">24/7 Rapid Response</span>
    </div>
  </div>

  <!-- FEATURES -->
  <section class="section">
    <div class="container">
      {features.map(f => (
        <div class={"feature" + (f.rev ? " rev" : "")}>
          <div class="feature-text">
            <h2>{f.title}</h2>
            <p>{f.body}</p>
            <a class="link" href={f.link.href}>{f.link.label} &rarr;</a>
          </div>
          <div class="feature-media">
            <img src={f.img} width="560" height="420" alt={f.alt} loading="lazy" />
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- SERVICES -->
  <section class="section" id="services" style="background:var(--cream-soft)">
    <div class="container">
      <div class="center-head">
        <span class="eyebrow">What we do</span>
        <h2>Security services built <em>around your site</em></h2>
      </div>
      <div class="cards">
        {site.services.map(s => (
          <div class="card">
            <div class="ic" set:html={icons[s.icon]} />
            <h3>{s.name}</h3>
            <p>{s.blurb}</p>
            <a class="link" href={s.href}>Learn more &rarr;</a>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- TESTIMONIAL -->
  <section class="section">
    <div class="container">
      <div class="quote-band">
        <span class="eyebrow">Client story · [CLIENT INPUT]</span>
        <blockquote>"Sentinel's guards turned up on time, looked the part, and our incidents dropped to zero. They feel like part of our team."</blockquote>
        <cite>Facilities Manager, Melbourne CBD property [placeholder — swap for a real review]</cite>
      </div>
    </div>
  </section>

  <!-- SECTORS -->
  <section class="section" id="sectors" style="background:var(--cream-soft)">
    <div class="container">
      <div class="center-head">
        <span class="eyebrow">Sectors we serve</span>
        <h2>Security tailored to <em>every kind of site</em></h2>
      </div>
      <div class="sectors">
        {site.sectors.map(s => (
          <a class="sector" href={s.href}>
            <img src={s.img} alt={s.alt} loading="lazy" />
            <span class="label">{s.name} &nearr;</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- DIFFERENCE -->
  <section class="section">
    <div class="container">
      <div class="center-head">
        <span class="eyebrow">The Sentinel difference</span>
        <h2>Built on trust, response <em>and real accountability</em></h2>
      </div>
      <div class="diff">
        <div class="col">
          <h3>Licensed &amp; insured</h3>
          <p>A current Victorian Private Security Business Licence, public liability cover and police-checked guards on every shift. Verify us with Victoria Police any time.</p>
        </div>
        <div class="col">
          <h3>Rapid local response</h3>
          <p>A 24/7 Melbourne dispatch team and GPS-verified patrols mean we deploy fast and prove it with real-time reporting, not vague promises.</p>
        </div>
        <div class="col">
          <h3>Transparent pricing</h3>
          <p>Clear hourly ranges up front and itemised quotes with no hidden loadings, so you know exactly what you're paying and why.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- HOW IT WORKS -->
  <section class="section" style="background:var(--olive);color:var(--cream);border-radius:var(--radius-lg)">
    <div class="container">
      <div class="center-head">
        <span class="eyebrow" style="color:var(--lime-soft)">How it works</span>
        <h2 style="color:var(--cream)">Protection in <em style="color:var(--lime)">four simple steps</em></h2>
      </div>
      <div class="diff">
        <div class="col" style="background:var(--olive-deep);border-color:rgba(255,255,255,.1)">
          <h3 style="color:var(--cream)">1. Assess</h3>
          <p style="color:#CFCDBC">We review your site and risks, on the phone or on site, fast.</p>
        </div>
        <div class="col" style="background:var(--olive-deep);border-color:rgba(255,255,255,.1)">
          <h3 style="color:var(--cream)">2. Quote</h3>
          <p style="color:#CFCDBC">You get a clear written quote with the right number of guards.</p>
        </div>
        <div class="col" style="background:var(--olive-deep);border-color:rgba(255,255,255,.1)">
          <h3 style="color:var(--cream)">3. Deploy</h3>
          <p style="color:#CFCDBC">Once approved, licensed guards are on site fast, in uniform.</p>
        </div>
      </div>
      <div style="text-align:center;margin-top:30px">
        <a class="btn btn-primary" href="/our-process/">See our full process &rarr;</a>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="section">
    <div class="container">
      <div class="center-head">
        <span class="eyebrow">Good to know</span>
        <h2>Security company Melbourne <em>FAQ</em></h2>
      </div>
      <div class="faq">
        {faqs.map(f => (
          <details>
            <summary>{f.q}</summary>
            <p>{f.a}</p>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="section" style="padding-top:0">
    <div class="cta-band">
      <span class="eyebrow">Ready when you are</span>
      <h2>Ready to protect <em>what matters?</em></h2>
      <p style="color:var(--muted);max-width:48ch;margin:8px auto 24px">Get a fast, free quote from a licensed Melbourne security company. We answer the phone 24/7.</p>
      <div style="display:flex;gap:14px;justify-content:center;flex-wrap:wrap">
        <a class="btn btn-dark" href="/contact/">Book a free quote</a>
        <a class="btn btn-ghost" href={site.phoneHref}>Call {site.phone}</a>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="container footer-grid">
      <div>
        <svg class="logo" viewBox="0 0 250 50" role="img" aria-label={site.brand}>
          <path d="M16 4l13 5v11c0 9-6.5 15.5-13 18-6.5-2.5-13-9-13-18V9l13-5z" fill="#C7D43F"/>
          <path d="M9.5 19.5l4.5 4.5 9-10" fill="none" stroke="#3B3B25" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
          <text x="40" y="26" font-family="Fraunces, Georgia, serif" font-size="21" font-weight="600" fill="#F5F1E8" letter-spacing="1">SENTINEL</text>
          <text x="41" y="40" font-family="Inter, sans-serif" font-size="8.2" font-weight="600" fill="#B9B7A6" letter-spacing="3">SECURITY · MELBOURNE</text>
        </svg>
        <p>Licensed, insured, 24/7 security guards, mobile patrols, event and commercial security across Greater Melbourne.</p>
        <p style="font-size:.82rem;color:#B9B7A6">{site.licenceNo}</p>
      </div>
      <div>
        <h4>Services</h4>
        <ul>
          {site.services.map(s => <li><a href={s.href}>{s.name}</a></li>)}
          <li><a href="/security-services-melbourne/">All services</a></li>
        </ul>
      </div>
      <div>
        <h4>Company</h4>
        <ul>
          <li><a href="/about/">About</a></li>
          <li><a href="/licensing-compliance/">Licensing &amp; compliance</a></li>
          <li><a href="/reviews/">Reviews</a></li>
          <li><a href="/service-areas/">Service areas</a></li>
          <li><a href="/blog/">Resources</a></li>
        </ul>
      </div>
      <div>
        <h4>Get in touch</h4>
        <ul>
          <li><a href={site.phoneHref}>{site.phone}</a></li>
          <li><a href={"mailto:" + site.email}>{site.email}</a></li>
          <li><a href="/contact/">Get a free quote</a></li>
          <li>{site.address}</li>
        </ul>
      </div>
    </div>
    <div class="container footer-bottom">
      <span>&copy; {new Date().getFullYear()} {site.brand}. All rights reserved.</span>
      <span><a href="/privacy-policy/">Privacy</a> &nbsp;·&nbsp; <a href="/terms-of-service/">Terms</a></span>
    </div>
  </footer>
</Base>
